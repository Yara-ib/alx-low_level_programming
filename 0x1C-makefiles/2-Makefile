CC := gcc
SRC := $(wildcard *.c)
OBJ := $(SRC:.c = .o)
NAME := school

all: ${NAME} clean

# gcc main.o school.o -o school
${NAME}: ${OBJ}
	${CC} $^ -o $@

# gcc -c -o school.o school.c
%.o: %.c
	${CC} -c -o $@ ${SRC}

clean:
	@rm -f ${OBJ}
