CC = gcc
SRC = $(wildcard %.c)
OBJ = $(SRC:.c=.o)
NAME = school
RM = rm -f

all: ${OBJ}
	${CC} ${OBJ} -o ${NAME}

clean:
	${RM} ${NAME} *~

oclean:
	${RM} *.o

fclean:
	${RM} *~ *.o ${NAME}

re: fclean all
