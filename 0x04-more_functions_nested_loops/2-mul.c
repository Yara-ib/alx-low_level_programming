#include "main.h"

/**
 * mul - Entry point
 *
 * @a: Parameter for mul()
 *
 * @b: Parameter for mul()
 *
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	int x = (a * b);

	return (x);
}
