#include "main.h"

/**
 * _puts - Entry point
 *
 * @str: Parameter for _puts()
 *
 * Return: Always 0 (Success)
 */

void _puts(char *str)
{
	int x = 0;

	if (*s != '\0')
	{
		rev_string(s + 1);
	}
	_putchar('\n');
}
